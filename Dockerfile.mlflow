FROM python:3.11-slim

RUN pip install --no-cache-dir mlflow psycopg2-binary boto3

EXPOSE 5000

CMD ["mlflow", "server", "--backend-store-uri", "postgresql://mlflow:mlflow@postgres:5432/mlflow", "--default-artifact-root", "s3://mlflow-artifacts/", "--host", "0.0.0.0", "--port", "5000"]
