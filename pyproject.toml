[project]
name = "maintain-x"
version = "0.1.0"
description = "Multimodal AI platform for predictive industrial maintenance"
readme = "README.md"
requires-python = ">=3.11,<3.14"

dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "xgboost",
    "matplotlib",
    "seaborn",
    "jupyter",
    "pyyaml",
    "pydantic",
    "fastapi",
    "uvicorn",
    "joblib",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "black",
    "pre-commit",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100